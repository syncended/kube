package dev.syncended.kube.styling

import dev.syncended.kube.core.model.toColor

object Colors {
  val white = "#ffffff".toColor()
  val red = "#ff0000".toColor()
  val green = "#00ff00".toColor()
  val blue = "#0000ff".toColor()
  val black = "#000000".toColor()

  val codeBlockBackground = "#FF0000".toColor()
}